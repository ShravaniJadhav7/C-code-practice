#include <stdio.h>
int main()
{
    int n = 10;
    int i;
    printf("Enter the number you want multiplication of: ");

    for (i = 11; i >= 1; i--)
    {

        printf("The multilpcation table of %d * %d = %d\n", 10, i, 10 * i);
    }

    return 0;
}